---
id: cg-a-norm-optimality-theorem
title: "Оптимальность метода сопряжённых градиентов в энергетической норме"
aliases: ["Минимизационное свойство CG", "Энергетическая проекция"]
type: theorem
status: canonical
publish: true
areas: [numerical-analysis, numerical-linear-algebra]
concepts: [conjugate-gradient, energy-norm, krylov-subspace, galerkin-orthogonality]
prerequisites: [conjugate-gradient-method, symmetric-positive-definite-matrix]
ai_domains: [gaussian-processes, second-order-methods]
source_refs:
  - id: tyrtyshnikov-numerical-analysis
    pages: "196-198"
    role: primary
level: advanced
created: 2026-07-15
updated: 2026-07-31
---

# Оптимальность метода сопряжённых градиентов в энергетической норме

## Зачем

Теорема объясняет название и короткую рекурсию метода: каждое приближение является не просто следующим шагом, а лучшей точкой во всём накопленном подпространстве в геометрии матрицы $A$.

## Формулировка

Пусть $A=A^*>0$, $x_*$ решает $Ax=b$, $r_0=b-Ax_0$. Приближение $x_k$ метода сопряжённых градиентов удовлетворяет

$$
x_k=\operatorname*{argmin}_{x\in x_0+\mathcal K_k(A,r_0)}
\|x-x_*\|_A,
$$

где $\|v\|_A^2=(Av,v)$. Эквивалентно,

$$
r_k\perp\mathcal K_k(A,r_0).
$$

В точной арифметике решение достигается не позднее $n$ шагов и раньше, если минимальный многочлен относительно $r_0$ имеет меньшую степень.

## Доказательство

1. Для ошибки $e=x-x_*$ квадратичный функционал удовлетворяет

$$
\phi(x)-\phi(x_*)=\frac12\|e\|_A^2.
$$

2. Минимум на аффинном подпространстве характеризуется нулевой производной по любому $y\in\mathcal K_k$:

$$
(Ae_k,y)=0.
$$

3. Поскольку $Ae_k=Ax_k-b=-r_k$, это эквивалентно $r_k\perp\mathcal K_k$.

4. Алгоритмическая рекурсия строит $A$-ортогональные направления, базис крыловского подпространства, и выбирает коэффициенты, обеспечивающие условие шага 3.

5. При $k=n$ подпространство содержит точную поправку; при меньшей степени минимального многочлена — раньше.

Геометрически для любого $y\in x_0+\mathcal K_k$ выполняется энергетическое разложение

$$
\|y-x_*\|_A^2
=
\|x_k-x_*\|_A^2+
\|y-x_k\|_A^2.
$$

Поэтому эллипсоид энергетического уровня впервые касается допустимого аффинного пространства именно в $x_k$.

## Контрпример

Если $A$ симметрична, но неопределённа, выражение $\|v\|_A$ не является нормой, а знаменатель $(Ap_k,p_k)$ может обратиться в нуль. Теорема неприменима.

## Вычислительная форма

Короткая рекурсия хранит несколько векторов, но точная ортогональность постепенно теряется. Периодический пересчёт невязки помогает контролировать накопленную ошибку.

## Перенос в ИИ

**Установленный результат.** Для положительно определённой линейной подзадачи каждый шаг даёт лучшую энергетическую ошибку в доступном подпространстве.

**Установленный результат.** Положительно определённая ядровая система гауссовского процесса является частным случаем такой линейной задачи. В ней оптимальность относится к $K$-норме и $K$-ортогональности, а не к произвольной метрике качества модели.

## Визуализация

![Энергетический эллипсоид касается аффинного пространства Крылова в лучшем приближении CG](80_assets/numerical-analysis/gpt-image-v6/cg-a-norm-optimality-v6.png)

### Как читать рисунок

Синий эллипсоид — множество равной энергетической ошибки, плоскость — все доступные на шаге $k$ поправки. Точка касания даёт минимум, а нормаль в энергетической геометрии кодирует условие Галёркина. Панель ядровой системы повторяет тот же установленный механизм с матрицей $K$. При неопределённой матрице уровни становятся гиперболическими, поэтому аргумент через ближайшую точку исчезает.

## Самопроверка

1. Почему градиент квадратичного функционала равен $Ax-b$?
2. Как связаны ортогональность невязки и энергетический минимум?
3. Что определяет досрочное точное завершение?

## Источник

[[60_sources/tyrtyshnikov-numerical-analysis|Тыртышников]], §19.7–19.8, стр. 196–198.
