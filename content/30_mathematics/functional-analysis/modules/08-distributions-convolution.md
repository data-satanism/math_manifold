---
id: fa-08-distributions-convolution
title: "Модуль 8. Обобщённые функции и свёртка"
aliases: ["FA 08", "Обобщённые функции"]
type: course-module
status: canonical
publish: true
areas: [functional-analysis, pde, harmonic-analysis]
concepts: [distribution, convolution, weak-derivative]
prerequisites: [fa-07-hilbert-operators]
ai_domains: [computer-vision, scientific-ml, signal-processing]
source_refs:
  - id: boss-fa-2005
    pages: "123-132"
    role: primary
level: advanced
created: 2026-07-10
updated: 2026-08-12
---

# Модуль 8. Обобщённые функции и свёртка

## Результаты обучения

После изучения модуля читатель сможет:

- определять обобщённую функцию через действие на тестовые функции;
- переносить производную на тестовую функцию с правильным знаком;
- проверять условия существования свёртки и её эквивариантность к сдвигу.

## Пререквизиты и маршрут зависимостей

тестовые функции → [[20_concepts/distribution|обобщённые функции]] → слабая производная → [[20_concepts/convolution|свёртка]] → функция Грина → операторные модели полей.

## Покрытие источника

Модуль покрывает соответствующую главу учебника Босса на с. 123–132. Постраничное соответствие подразделов сохранено в [[30_mathematics/functional-analysis/source-map|карте источника]].


## Зачем это нужно

Классическая производная исчезает на разрывах, а импульсы не являются обычными функциями. [[20_concepts/distribution|Распределения]] расширяют язык так, чтобы дифференцирование, свёртка и уравнения в частных производных оставались осмысленными.

## 1. Тестовые функции

Пространство $\mathcal D(\Omega)=C_c^\infty(\Omega)$ снабжается топологией, контролирующей общий компактный носитель и равномерную сходимость всех производных. Распределение — непрерывный линейный функционал $T : \mathcal D\to\mathbb F$.

Непрерывность здесь нельзя заменить обычной непрерывностью по норме: $\mathcal D$ задаётся семейством полунорм и топологией индуктивного предела.

## 2. Регулярные и сингулярные распределения

Локально интегрируемая функция $f$ задаёт $T_f(\varphi)=\int f\varphi$. Дельта-распределение $\delta_{x_0}(\varphi)=\varphi(x_0)$ сингулярно: оно сосредоточено в точке и не имеет плотности относительно меры Лебега.

## 3. Дифференцирование

Интегрирование по частям подсказывает определение

$$
\langle D^\alpha T,\varphi\rangle=(-1)^{|\alpha|}\langle T,D^\alpha\varphi\rangle.
$$

Граничный член исчезает из-за компактного носителя тестовой функции. Это даёт слабые производные и позволяет трактовать уравнения в частных производных в смысле распределений.

## 4. Свёртка

[[20_concepts/convolution|Свёртка]] регулярных функций задаётся интегралом. Для распределения $T$ и тестовой функции $\varphi$

$$
(T*\varphi)(x)=T(\varphi(x-\cdot)).
$$

Свёртка сглаживает: распределение после свёртки с гладкой функцией компактного носителя становится гладким. Аппроксимирующие единицы восстанавливают исходный объект в подходящей топологии.

## 5. Дифференциальные уравнения и функция Грина

Если $LG=\delta$, то решение $Lu=f$ формально имеет вид $u=G*f$. Корректность требует области определения, граничных условий и существования свёртки. Оператор Грина превращает дифференциальное уравнение в операторное.

## 6. Расходящиеся ряды

Последовательность может расходиться поточечно, но сходиться в смысле распределений после тестирования. Это полезно, но слабый предел нельзя автоматически подставлять в нелинейную операцию: произведение распределений вообще не определено.

## Механизм доказательства сглаживания

Для $T\in\mathcal D'(\Omega)$ и тестовой функции $\varphi$ зависимость $x\mapsto\varphi(x-\cdot)$ гладка в топологии тестовых функций. Непрерывность распределения разрешает дифференцировать действие по параметру:

$$
\partial^\alpha(T*\varphi)(x)=T\!\left(\partial^\alpha\varphi(x-\cdot)\right)=T*(\partial^\alpha\varphi)(x).
$$

Все производные существуют и непрерывны, поэтому $T*\varphi\in C^\infty$. Компактность носителя нужна, чтобы сдвинутые тестовые функции оставались в допустимом пространстве на рассматриваемой области.

## Что может пойти не так

Свёртка двух произвольных распределений не обязана быть определена: достаточно, например, компактного носителя одного из множителей или более специальных условий. Произведение распределений также не существует в общем случае, поэтому нелинейное уравнение нельзя получать формальной подстановкой слабого предела.

## Вычислительный эксперимент

Сверните дискретную аппроксимацию дельта-импульса с гауссовыми ядрами разных ширин и сравните производную результата с результатом свёртки производной ядра. Повторите опыт с нулевым, периодическим и отражающим дополнением границы: различие на краях показывает, где дискретная свёртка теряет непрерывную эквивариантность.

## Связь с ИИ

[[50_bridges/distributions-convolution|Свёртка, CNN и нейронные операторы]] показывает, какие свойства инвариантности наследуются от непрерывного оператора, а какие нарушаются дискретизацией и дополнением границы.

## Визуализация

![Обобщённые функции, свёртка и перенос в ИИ — модуль 08](80_assets/functional-analysis/modules/gpt-image-v3/module-08-distributions-convolution-v3.png)


## Самопроверка

1. Почему дельта-функция не является обычной функцией?
2. Откуда возникает знак $(-1)^k$ при дифференцировании распределения?
3. Какие граничные условия нарушают точную эквивариантность дискретной свёртки?

## Упражнения

1. Докажите $H'=\delta$ для функции Хевисайда.
2. Выведите перестановочность $D(T*\varphi)=T*D\varphi$.
3. Объясните, почему $\delta^2$ не определено в классической теории распределений.

Далее: [[30_mathematics/functional-analysis/modules/09-operator-equations]].
